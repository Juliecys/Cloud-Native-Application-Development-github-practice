name: Successful CI Test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run a simple command
        run: echo "Hello, this is a successful step!"  # 這個步驟會成功執行

      - name: Another successful step
        run: echo "This step also succeeds!"  # 這個步驟也會成功
